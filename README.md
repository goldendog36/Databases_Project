# Databases_Project

We will be doing our databases project and uploading important files to it here!
Huzzah! Yippee!

I made it a lot more efficient since last time but it still takes a while to calculate the 14-day rsi values so if someone could implement that as a python script, that would be preferable.

To create the tables, run mysql:
sudo mysql -u root < tables.sql

Then run other sql statements in a python file which we can probably turn into our cli or other implementation after a while.
